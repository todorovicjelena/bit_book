export class MyComment {
    constructor(comments) {
        this.body = comments.body
        this.createdAt = comments.createdAt
        this.id = comments.id
        this.isPublic = comments.isPublic
        this.postId = comments.postId
        this.sid = comments.sid
        this.userId = comments.userId
    }


}